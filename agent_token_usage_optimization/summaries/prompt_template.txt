You are summarizing a source file for an automated code-search index.
Your summary will be read by AI coding agents (Claude, Codex, Antigravity,
etc.) to decide whether to open the full file. Every wasted token in your
summary costs an agent context budget. Be terse, factual, and specific.

OUTPUT FORMAT — emit EXACTLY these sections, in this order, as plain
markdown. No preamble. No closing remarks. No code fences around the whole
response. No restating of the filename. Total response under 250 words.

## PURPOSE
One sentence, max 25 words. What this file does in the system. Start with
a verb. No filler like "This file" / "This module".

## WHEN TO READ THIS FILE
One sentence, phrased as a decision rule:
"Read this if you need to ___."
This is the single most important line — agents use it to decide whether
to open the file at all.

## NOT USEFUL FOR
One sentence naming the nearest commonly confused responsibility that is
handled elsewhere. Format: "Not for ___." Write `none` if no meaningful
distinction is evident from the contents.

## KEY EXPORTS
Bullet list, max 8 items. One per line, format:
- `Qualified.name(args)` — one-line description of what it does or returns.
Use exact identifiers and signatures where visible; include symbol kind when
the name alone is ambiguous.
Skip private helpers. If the file exports nothing public, write the
single line: none.

## NAVIGATION
Bullet list, max 8 items. Point agents to the highest-value locations using:
- `<role>` — `<exact qualified symbol or unique searchable anchor>` — approx lines `<start>-<end>`
Prioritize entrypoints, main control flow, validation, persistence/network
boundaries, and error/fallback paths. INCLUDE private control-plane methods
when they own the real work (e.g. `_fetchX`, `_handleSave`). Include useful
cross-file jumps as `calls/called by -> path::symbol`. Line numbers are
approximate only — a post-processor will replace them from the symbol outline.
Never give a line range without an exact symbol or unique anchor.
Write `none` only when the file has no meaningful internal navigation points.

## NEXT HOP
Exactly one line. Best first symbol to open for a typical change to this file:
`broker.py read {path} --symbol <ExactName>`
Use the real path of this file for {path}. If no single symbol fits:
`broker.py outline {path}`

## INTERNAL DEPENDENCIES
Comma-separated list of INTERNAL modules this file imports (relative
paths within the repo, or first-party package-internal names). Max 10
items. Prefer `path::symbol` when a specific dependency is central. Write
"none significant" if trivial.

## EXTERNAL SYSTEMS
Comma-separated list of third-party frameworks, services, protocols, data
stores, or platform APIs central to behavior. Use exact names. Skip incidental
libraries and stdlib. Write `none significant` if trivial.

## SIDE EFFECTS
Comma-separated, choose from: database writes, database reads,
network calls, filesystem, process state, none.
Add a max-5-word qualifier per item if non-obvious, e.g.,
"database writes (upserts crossword masters)".

## HIDDEN CONTRACTS & FAILURE MODES
Bullet list, max 4 items. ONLY non-obvious things a reader could miss:
ordering requirements, validation assumptions, fallback behavior, partial
failure, retry/idempotency rules, or known footguns. If none, write a single line: none.
DO NOT pad this section. Empty is acceptable and preferred over filler.

## DOMAIN CONCEPTS
Comma-separated domain terms this file centers on (e.g.,
"crossword grid, NK hash, master sync"). Max 6 terms. This drives
semantic search across the repo. Use the repo's actual terminology, not
generic ones. If the file is purely generic infrastructure
(logging, config loading, etc.), write: generic.

HARD RULES (violations make the summary useless):
- No prose outside the listed sections.
- No "this file" / "this module" filler.
- If a section truly has nothing to say, write `none`. Never invent.
- Preserve exact casing and spelling for symbols, routes, config keys,
  environment variables, database entities, events, and error anchors.
- Prefer stable symbols and unique text anchors over line numbers.
- Always emit NEXT HOP with a real symbol name when any export or method exists.
- Do not summarize the imports list as content.
- Do not describe coding style, formatting, or language features.
- Do not speculate about future work or refactoring opportunities.

------------------------------------------------------------
FILE METADATA
  path:     {path}
  language: {lang}
  loc:      {loc}
------------------------------------------------------------

<<<FILE CONTENTS BEGIN>>>
{content}
<<<FILE CONTENTS END>>>
