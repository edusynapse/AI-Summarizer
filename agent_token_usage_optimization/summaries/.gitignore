# intentionally empty for summaries/repo/ + manifest — commit those if you want
# teammates to inherit the LLM cache.
#
# Embeddings index is large/binary and backend-specific — do not commit:
embeddings/
embeddings/index.json
embeddings/vectors.bin
